'use client';
import ZkEmailComponent from '@/components/page/zk-email';
import { Box, Text } from '@chakra-ui/react';
export default function ZkEmailPage() {
    return (
        <Box className="min-h-screen w-full relative" >
            <ZkEmailComponent   ></ZkEmailComponent>
        </Box>
    );
}
